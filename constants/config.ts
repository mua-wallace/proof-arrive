// Center ID - This should be configured per installation
// For now, using a default value. In production, this could come from:
// - Environment variables
// - Device storage
// - User login/authentication
export const DEFAULT_CENTER_ID = 'CENTER-001';

// Agent ID - This should come from user authentication
// For now, using a default value
export const DEFAULT_AGENT_ID = 'AGENT-001';

